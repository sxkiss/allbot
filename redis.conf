# Redis数据持久化目录
dir /data/redis

# RDB持久化：60秒内有1000次写操作则保存
save 60 1000

# 启用AOF持久化
appendonly yes

# AOF同步策略：每秒同步一次（平衡性能和安全）
appendfsync everysec

# 绑定所有网络接口
bind 0.0.0.0

# 保护模式关闭（容器内使用）
protected-mode no