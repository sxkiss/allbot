# 869 平台微信 CDN 文件下载方式报告

> 撰写日期：2026-08-07
> 验证环境：主机 `127.0.0.1:5253`（wxserver 容器），账号 `wxid_67o8m516qre022`（Hdkhd / 与你相约）
> 下载通道：869 提供的 `SendCdnDownload` 接口（单接口、不可多线程，单次请求即返回完整文件）

---

## 一、结论速览

| 文件类型 | 有效 FileType | 耗时 | 返回码 | 结果 |
|---------|--------------|------|--------|------|
| 大文件（145MB APK） | **`7`** | ~60-70s（缓存后 ~5s） | `RetCode=0` | ✅ MD5 匹配、APK 头正确 |
| 小文件（115KB APK） | **`5`** | ~0.3s | `RetCode=0` | ✅ MD5 匹配 |

**核心发现：** 小文件用 `FileType=5`，大文件必须用 `FileType=7`。`6/8/3` 等类型对大文件均失败（`0xFFB22257`）。

---

## 二、接口调用

```bash
curl -X POST "http://127.0.0.1:5253/message/SendCdnDownload?key=HB8zd4SN4anP" \
  -H "Content-Type: application/json" \
  -d '{"AesKey":"<aeskey>","FileType":7,"FileURL":"<cdnattachurl>"}'
```

### 参数说明
- `AesKey`：消息 XML 中的 `<aeskey>`，小写 hex 字符串
- `FileType`：**7（大文件）/ 5（小文件）**
- `FileURL`：**纯 `<cdnattachurl>`**，【不要】加 `@cdn_` 前缀，【不要】拼接 aeskey
- `key`：869 平台鉴权 key

### 返回解析
响应为 JSON，`Data.FileData` 是 **base64 编码、且已 AES-ECB 解密** 的原始文件内容：

```python
import json, base64, urllib.request

body = json.dumps({
    "AesKey": aeskey,
    "FileType": 7,
    "FileURL": cdnattachurl,   # 纯 URL
}).encode()
req = urllib.request.Request(API, data=body, headers={"Content-Type": "application/json"})
with urllib.request.urlopen(req, timeout=600) as r:
    resp = json.loads(r.read())
d = resp["Data"]
if d["RetCode"] == 0:
    raw = base64.b64decode(d["FileData"])
    open(out_path, "wb").write(raw)
```

---

## 三、正确参数提取（从消息 XML）

从 allbot 主库 `database/message.db` 表 `messages` 的文件消息中提取：
- `<aeskey>` → `AesKey`
- `<cdnattachurl>` → `FileURL`（纯值，无 `@cdn_`）
- `<totallen>` → 期望文件大小
- `<md5>` → 校验

**注意：** 消息可能有多条重复记录（同一 msg_id 含多种 xml 版本），务必取含完整 `cdnattachurl`/`aeskey` 的版本。

### 实际验证样本（私聊，sxkiss_com → Hdkhd）
| 文件 | msg_id | aeskey | FileType | MD5 |
|------|--------|--------|----------|-----|
| 鹿-精灵_V60.11.01_AI录屏直-.apk.1 | 512907308 | `a3d3835fa0846f49fece48b693e16ff3` | 7 | `86932446577db21ad27997c0557f78d6` |
| wechat-observatory-android-v0.1.9-debug.apk.1 | 444295639 | `f6cac2928b35481169a6d32eb015bb58` | 5 | `dd8190f14bd2eb6ccb7ca8f5c35ac2eb` |

---

## 四、错误码对照

| RetCode | 说明 |
|---------|------|
| `0` | 成功，`FileData` 含完整数据 |
| `4289864279` (`0xFFB22257`) | FileURL 参数错误 / 文件未找到 / FileType 不匹配（常见于**类型用错**） |
| `4289864130` (`0xFFB221C2`) | CDN 需重试（`IsRetry=1`），常因用了 `@cdn_` 前缀触发 |
| 返回 `Data=null, Success=false` | 接口侧错误（如 URL 拼接了 `_aeskey` 导致服务端解密失败） |
| `4289864094` | 大小超过限制 |

---

## 五、踩坑记录（务必避开）

1. **不要拼接 aeskey 到 URL 尾部**：`<url>_<aeskey>` 会让返回 `Data=null, Success=false`。
2. **不要用 `@cdn_` 前缀**：`attachid` 字段含 `@cdn_`，但下载要用**纯 `cdnattachurl`**。
3. **大文件必须 `FileType=7`**：`5/6/8/3` 均报 `0xFFB22257` 或重试。
4. **单接口、串行**：接口不支持多线程并发分片，单次请求即返回完整文件。
5. **超时**：145MB 首次下载约 60-70s，接口超时建议 ≥ 600s（10 分钟）。
6. **CDN 缓存**：同一文件重复请求会加速（缓存命中约 5s）。

---

## 六、结果文件位置

- 大件：`/home/sxkiss/鹿-精灵_V60.11.01_AI录屏直-.apk.1`（151,650,975 字节，MD5 校验通过）
- 小件：`/home/sxkiss/wechat-observatory-android-v0.1.9-debug.apk.1`（114,900 字节，MD5 校验通过）
